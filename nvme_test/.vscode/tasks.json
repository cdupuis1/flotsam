{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "make -C/lib/modules/`uname -r`/build M=$PWD modules",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Clean",
            "type": "shell",
            "command": "make -C/lib/modules/`uname -r`/build M=$PWD clean",
            "problemMatcher": []
        }
    ]
}